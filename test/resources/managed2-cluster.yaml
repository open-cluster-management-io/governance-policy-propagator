apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  labels:
    cloud: auto-detect
    name: managed2
    vendor: auto-detect
  name: managed2
  namespace: managed2
spec:
  hubAcceptsClient: true
